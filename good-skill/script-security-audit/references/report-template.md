# 安全审计报告模板

最终审计报告使用此结构。

---

# 安全审计报告

**项目：** [项目名称]
**日期：** [YYYY-MM-DD]
**范围：** [审计的目录/文件]
**语言：** [分析的语言]

## 概述

[1-2 段总结：发现总数、关键问题、整体风险评估]

| 严重程度 | 数量 |
|----------|------|
| 高       | X    |
| 中       | X    |
| 低       | X    |

## 发现

### [发现编号] [标题]

**严重程度：** 高 / 中 / 低
**安全问题类别：** [命令注入 / 上传下载 / 跨目录解压 / 跨目录读写 / 操作任意文件 / SQL 注入 / 完整性校验缺失 / 修改配置未校验参数 / 跨脚本污点传播 / 文件权限劫持]
**文件：** `path/to/file:行号`
**Sink 类型：** [如 system()、subprocess.run()、exec.Command()]
**参数来源：** [外部输入 / 内部常量 / 不确定，需人工确认]
**参数追溯路径：** [如：`system(cmd)` ← `cmd = sprintf(..., req.param("file"))` ← HTTP 请求参数]

**代码：**
```[语言]
[漏洞代码片段]
```

**完整分析过程：**
[记录从 sink 点发现到最终判定的完整分析链路，包括：]
1. Sink 点发现：grep 匹配到的原始行
2. 上下文读取：函数签名、参数列表、条件守卫
3. 参数溯源：向上追溯的路径和每层的判定
4. 命令解析：识别出的变量插值点和危险命令
5. cmd-*.md 比对：加载了哪些参考文件，匹配到哪些高危模式
6. 消毒检测：是否发现消毒，消毒方式和强度判定
7. 污点传播链（如有）：跨脚本追踪的完整路径
8. 最终判定：基于以上分析得出的风险等级和安全问题类别

**描述：**
[漏洞是什么以及如何运作]

**攻击场景：**
[攻击者如何利用此漏洞的具体示例]

**可达路径：**（仅当同一漏洞有多条可达路径时列出，否则删除此字段）
- 路径 1: `sink_a (file1.c:10)` → `script.sh`
- 路径 2: `sink_b (file2.c:25)` → `script.sh`

**污点传播链：**（仅当涉及跨脚本传播时填写，否则删除此字段）
```
sink点 (file.c:42) → scriptA.sh:$1 (line 5) → scriptB.sh:$1 (line 12) → rm -rf "$1"
```

---

（对每个发现重复，按 高 → 中 → 低 排序）

## 附录：Sink 点清单

| # | 文件 | 行号 | Sink 函数 | 风险等级 | 参数来源 | 备注 |
|---|------|------|-----------|----------|----------|------|
| 1 | path/to/file | 42 | system() | 高 | 外部输入 | bash -c 带用户输入 |
| 2 | path/to/file | 87 | subprocess.run | 低 | 内部常量 | 硬编码命令 |
| 3 | path/to/file | 120 | popen() | 高 | 不确定 | 需人工确认参数来源 |

## 反思与自检

### 审计覆盖范围
- 检测到的语言：[如 C/C++、Python]
- 扫描的文件数：[X]
- 发现的 Sink 点总数：[X]（高 [X] / 中 [X] / 低 [X]）
- 进入深度审计的 Sink 点数：[X]
- 跨脚本追踪达到最大深度的数量：[X]
- cmd-*.md 未覆盖的命令：[列出]

### 可能的误报
| # | 发现编号 | 文件:行号 | 误报理由 |
|---|---------|-----------|---------|
| 1 | F-001 | file.c:42 | 参数虽含变量但实际来自内部配置，追溯链不够深未能确认 |

### 需人工确认清单
| # | 发现编号 | 文件:行号 | 确认原因 |
|---|---------|-----------|---------|
| 1 | F-003 | file.c:120 | 参数来源不确定，来自框架回调函数链 |

### 审计局限性
- 跨脚本递归追踪最大深度 3 层，更深层调用链未覆盖
- [其他局限性]
